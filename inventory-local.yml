---
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
  children:
    kubernetes:
      hosts:
        localhost:
    kube_master:
      hosts:
        localhost:
    kube_workers:
      hosts:
        localhost:
